import ConfigParser

config = ConfigParser.ConfigParser()
config.readfp(open('Settings.cfg'))

	config.add_section('CloudStorage')
	dirloc = raw_input("Please enter the cloud stroage directory location:")
	with open('Settings.cfg', 'wb') as configfile:	
		config.set('CloudStorage', 'directory_location', dirloc)
		config.write(configfile)

def main():
#Print List of Directories

if __name__ == "__main__":
	main()
